/**
 * Generates a random number between 0 and 100 (inclusive).
 *
 * @returns {number} The generated random number.
 */
export function get_random_number(): number {
	return Math.floor(Math.random() * 100);
}
